<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LocalLiftLog.Pages.WorkoutDetailsPage"
             xmlns:viewmodel="clr-namespace:LocalLiftLog.ViewModels"
             xmlns:models="clr-namespace:LocalLiftLog.Models"
             x:DataType="viewmodel:WorkoutDetailsViewModel"
             Title="Workout Details Page">
    <VerticalStackLayout HorizontalOptions="Center">
        <HorizontalStackLayout 
            Spacing="5">
            <Label 
                Text="Workout Template Id:"
                FontSize="30"
                FontAttributes="Bold" />
            <Label 
                Text="{Binding WorkoutTemplate.Id}"
                FontSize="30"
                FontAttributes="Bold" />
        </HorizontalStackLayout>

        <HorizontalStackLayout 
            Spacing="5">
            <Label 
                Text="Set List Id:"
                FontSize="30"
                FontAttributes="Bold" />
            <Label 
                Text="{Binding WorkoutTemplate.SetTemplateCollectionId}"
                FontSize="30"
                FontAttributes="Bold" />
            <Button 
                Text="Reset"
                Style="{StaticResource DeleteButtonStyle}"
                Command="{Binding ResetSetTemplateCollectionIdCommand}" />
        </HorizontalStackLayout>

        <CollectionView
            ItemsSource="{Binding SetList}"
            HorizontalOptions="Center">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout 
                    ItemSpacing="3"
                    Orientation="Vertical" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:SetTemplate">
                    <HorizontalStackLayout Spacing="5">
                        <Label VerticalOptions="Center">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span 
                                        Text="Id: " 
                                        FontAttributes="Bold"  
                                        Style="{StaticResource ProgramSpanStyle}" />
                                    <Span 
                                        Text="{Binding Id}"
                                        Style="{StaticResource ProgramSpanStyle}" />
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <ContentView>
                    <VerticalStackLayout
                        HorizontalOptions="Center"
                        VerticalOptions="Center">
                        <Label 
                            Text="Set List Is Empty!" 
                            FontSize="18" 
                            FontAttributes="Bold" 
                            HorizontalTextAlignment="Center" />
                    </VerticalStackLayout>
                </ContentView>
            </CollectionView.EmptyView>
        </CollectionView>

        <Button 
            Text="Create New Set List"
            Style="{StaticResource AddButtonStyle}"
            WidthRequest="300"
            Command="{Binding CreateNewSetListCommand}"
            Margin="50" />

        <Button 
            Text="Go Back"
            Style="{StaticResource ProgramButtonStyle}"
            Command="{Binding GoBackCommand}"
            Margin="50" />
    </VerticalStackLayout>
</ContentPage>