<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LocalLiftLog.Pages.ExerciseListPage"
             xmlns:viewmodel="clr-namespace:LocalLiftLog.ViewModels"
             xmlns:models="clr-namespace:LocalLiftLog.Models"
             x:DataType="viewmodel:ExerciseListViewModel"
             Title="Exercise List">
    <VerticalStackLayout HorizontalOptions="Center">
        <VerticalStackLayout>
            <Label 
                Text="Exercise List"
                FontSize="30"
                FontAttributes="Bold"
                Margin="0, 10" />
            <CollectionView
                ItemsSource="{Binding ExerciseList}"
                HorizontalOptions="Center">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout 
                        ItemSpacing="5"
                        Orientation="Vertical" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Exercise">
                        <HorizontalStackLayout Spacing="10">
                            <Label VerticalOptions="Center">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span 
                                            Text="Name: " 
                                            FontAttributes="Bold"  
                                            Style="{StaticResource ProgramSpanStyle}" />
                                        <Span 
                                            Text="{Binding Name}"
                                            Style="{StaticResource ProgramSpanStyle}" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                            <Label VerticalOptions="Center">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span 
                                            Text="Exercise Groups: " 
                                            FontAttributes="Bold"  
                                            Style="{StaticResource ProgramSpanStyle}" />
                                        <Span 
                                            Text="{Binding ExerciseGroupSet, Converter={StaticResource ExerciseGroupConverter}}"
                                            Style="{StaticResource ProgramSpanStyle}" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>

        <VerticalStackLayout
            HorizontalOptions="Center"
            Margin="50"
            Spacing="5">
            <Label VerticalOptions="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span 
                            Text="Filtered Exercise Groups: " 
                            FontAttributes="Bold"  
                            Style="{StaticResource ProgramSpanStyle}" />
                        <Span 
                            Text="{Binding ExerciseGroupFilterSet, Converter={StaticResource ExerciseGroupConverter}}"
                            Style="{StaticResource ProgramSpanStyle}" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <HorizontalStackLayout Spacing="15">
                <Entry 
                    Text="{Binding ExerciseGroupFilterInput}"
                    Style="{StaticResource SmallEntryFieldStyle}" />
                <Button 
                    Text="Filter Exercise List"
                    Command="{Binding AddExerciseGroupToFilterListCommand}"
                    CommandParameter="{Binding ExerciseGroupFilterInput}"
                    Style="{StaticResource ProgramButtonStyle}" />
                <Button 
                    Text="Reset Exercise List Filter"
                    IsVisible="{Binding ExerciseGroupFilterSet.Count}"
                    Command="{Binding ResetFilterListCommand}"
                    Style="{StaticResource DeleteButtonStyle}" />
            </HorizontalStackLayout>
        </VerticalStackLayout>

        <VerticalStackLayout>
            <Button 
                Text="Go Back"
                Style="{StaticResource ProgramButtonStyle}"
                Command="{Binding GoBackCommand}"
                Margin="50" />
        </VerticalStackLayout>
    </VerticalStackLayout>
</ContentPage>